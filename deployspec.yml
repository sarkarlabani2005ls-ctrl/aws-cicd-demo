version: 0.1

files:
  - source: /index.html
    destination: /var/www/html/

scripts:
  BeforeDeploy:
    - location: scripts/install-nginx.sh
      timeout: 300
      runas: root

  AfterDeploy:
    - location: scripts/start-nginx.sh
      timeout: 300
      runas: root
